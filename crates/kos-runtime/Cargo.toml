[package]
name = "kos-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
kos-core = { path = "../kos-core" }
kos-context = { path = "../kos-context" }
kos-compute = { path = "../kos-compute" }

serde = { version = "1", features = ["derive"] }
thiserror = "2"

