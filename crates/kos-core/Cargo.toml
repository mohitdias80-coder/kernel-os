[package]
name = "kos-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Core types and ABI for Kernel OS"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }

